interface:
  display_name: "Unfog"
  short_description: "Turn vague requests into verified execution contracts"
  default_prompt: "Use $unfog to turn this request into a verified execution contract and complete it."
policy:
  allow_implicit_invocation: true
